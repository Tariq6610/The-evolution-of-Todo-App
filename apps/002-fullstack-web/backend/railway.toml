[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn src.main:app --host 0.0.0.0 --port $PORT"
restartOnDeploy = true
numReplicas = 1
sleepApplication = false

[variables]
PYTHON_VERSION = "3.11"